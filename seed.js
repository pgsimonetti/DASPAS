require('dotenv').config();const bcrypt=require('bcryptjs');const {Pool}=require('pg');const p=new Pool({connectionString:process.env.DATABASE_URL});(async()=>{const h=await bcrypt.hash(process.env.ADMIN_PASSWORD||'Cambiar123!',12);await p.query(`INSERT INTO usuarios(nombre,email,password_hash,rol) VALUES($1,$2,$3,'admin') ON CONFLICT(email) DO UPDATE SET nombre=EXCLUDED.nombre,password_hash=EXCLUDED.password_hash,rol='admin'`,['Administrador',process.env.ADMIN_EMAIL||'admin@daspas.com',h]);console.log('Usuario administrador creado/actualizado');await p.end()})().catch(e=>{console.error(e);process.exit(1)})
